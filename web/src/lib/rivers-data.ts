// Static river definitions — re-exported from the shared workspace package.
// Live CFS, trend, and runnable status are fetched in rivers.ts via USGS.
export type { DifficultyClass, RiverStatic } from "@riverrats/shared";
export { riversData } from "@riverrats/shared";
